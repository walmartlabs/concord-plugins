configuration:
  runtime: "concord-v1"
  dependencies:
    - "mvn://com.walmartlabs.concord.plugins:terraform-task:%%version%%"

flows:
  default:
    - task: terraform
      in:
        action: plan
